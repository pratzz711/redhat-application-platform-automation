name: Deploy to Openshift

on:
  push:
    branches:
      main
    paths:
      - 'openshift/**'
    
jobs:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4